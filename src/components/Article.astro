---
interface Props {
    title?: string;
}

const { title = "Title" } = Astro.props;
---

<div class={'flex flex-col gap-8 w-full mb-20'}>
    <div class="text-center">
        <p class="text-3xl font-bold">{title}</p>
    </div>

    <slot/>
</div>